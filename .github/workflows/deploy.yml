name: "Deploy"
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
      - name: Setup services
        run: docker-compose up -f docker-compose.yml -f docker-compose.prod.yml -d --remove-orphans
